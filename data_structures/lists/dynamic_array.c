#include <stdio.h>

typedef struct darray_s {
  int size;
  int *data;
} darray_t;

darray_t* create_darray(int size) {
  return NULL;
}

void darray_append(darray_t *arr, int val);
int darray_pop(darray_t *arr);

int darray_get(int i);
void darray_set(int i, int val);

int main () {

  
  
  printf("hi\n");
}
